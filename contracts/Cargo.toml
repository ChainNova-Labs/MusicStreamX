[workspace]
members = [
    "shared",
    "music-nft",
    "streaming",
    "governance",
    "fan-tokens",
]

[workspace.dependencies]
soroban-sdk = "20.0.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

[profile.dev]
opt-level = 0
debug = true
